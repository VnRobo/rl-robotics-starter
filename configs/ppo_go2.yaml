robot: go2
algo: ppo
timesteps: 1_000_000
n_envs: 4
seed: 42
save_dir: ./checkpoints

ppo:
  n_steps: 2048
  batch_size: 64
  n_epochs: 10
  gamma: 0.99
  gae_lambda: 0.95
  clip_range: 0.2
  ent_coef: 0.01
  learning_rate: 3.0e-4
